package com.dartsapp.ui.theme

import androidx.compose.ui.graphics.Color

// Hintergründe (sehr dunkel, leicht blauer Ton)
val DartBackground     = Color(0xFF0D0D1A)  // Fast schwarz, minimaler Blaustich
val DartSurface        = Color(0xFF13132A)  // Dunkles Navy für Cards/Sheets
val DartSurfaceVariant = Color(0xFF1C1C3D)  // Etwas heller für erhöhte Flächen

// Primary – Dunkles Lila
val DartPurple         = Color(0xFF6A0DAD)  // Tiefes Lila
val DartPurpleLight    = Color(0xFF9B59B6)  // Mittleres Lila (interaktive Elemente)

// Accent – Dunkles Blau / Indigo
val DartBlue           = Color(0xFF1A237E)  // Tiefes Indigo-Blau
val DartBlueLight      = Color(0xFF5C6BC0)  // Mittleres Indigo (Akzente, Icons)

// Text / Content
val DartOnBackground   = Color(0xFFE8EAF6)  // Fast Weiß mit leichtem Blaustich
val DartOnSurface      = Color(0xFF9FA8DA)  // Gedämpftes Indigo-Grau

// Funktionale Farben
val DartRed            = Color(0xFFB71C1C)  // Bust / kritisch
val DartRedLight       = Color(0xFFEF5350)  // Error-State
val DartGold           = Color(0xFFFFD54F)  // Sieger-Highlight
